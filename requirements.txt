# Collecte : aucune dépendance (urllib de la stdlib suffit pour lire le GBFS).
# Analyse & export :
pandas>=2.0
numpy>=1.24
pyarrow>=14          # export Parquet (sinon repli CSV automatique)
matplotlib>=3.7      # graphiques du rapport d'étude de marché (analyze.py)
scikit-learn>=1.4    # modèle de demande (ml.py)
scipy>=1.11          # densités par rayon GPS (ml.py, transitive via scikit-learn mais utilisée directement)

# Dashboard local (webapp/) :
fastapi>=0.110
uvicorn>=0.29
